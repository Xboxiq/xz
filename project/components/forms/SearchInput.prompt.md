Search box with a leading icon that warms to crimson on focus.

```jsx
<SearchInput placeholder="ابحث برقم اشتراك، اسم، أو رقم خدمة…" />
<SearchInput hero placeholder="ابحث في كل الخدمات" />
```

`hero` is the oversized landing-page variant. `icon` overrides the leading glyph.
