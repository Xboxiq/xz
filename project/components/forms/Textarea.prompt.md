Multi-line notes field — vertically resizable, loose line-height for Arabic case notes.

```jsx
<Field label="ملاحظات الموظف">
  <Textarea placeholder="سجّل تفاصيل الطلب…" rows={4} />
</Field>
```

Same crimson focus ring as `Input`. Min-height ~130px, resizes vertically.
